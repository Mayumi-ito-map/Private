### ② スクリプト構造整理
```
project/
│
├── config/
│   ├── overseas_territories.json
│   └── 自然地名_変換表.xlsx
│
├── normalizers/
│   ├── normalize_comma_abb.py
│   └── __init__.py
│
├── matchers/
│   ├── stage_matcher.py   ← matching_sample整理版
│   └── __init__.py
│
├── scripts/
│   ├── build_local_candidates.py   ← 司令塔①
│   └── run_stage_match.py          ← 司令塔②
│
├── output_match/
├── output_match_results/
└── output_json/


解決方法（おすすめ順）:
1. project直下に戻って実行する。

cd ..
python -m scripts.run_stage_match

2. とりあえず動かす方法:
run_stage_match.py の一番上にこれを入れる：
```
import sys
from pathlib import Path

sys.path.append(str(Path(__file__).resolve().parent.parent))
```


---

project/
│
├── config/
│   ├── overseas_territories.json
│
├── normalizers/
│   ├── normalize_comma_abb.py
│   └── __init__.py
│   └── 自然地名_変換表.xlsx       
│
├── matchers/
│   ├── stage_matcher.py   ← matching_sample整理版
│   └── __init__.py
│
├── run_build_local.py     ← 司令塔①
├── run_stage_match.py      ← 司令塔②
│
├── output_match/
├── output_match_results/
└── output_json/

こうした。
project_new/
├── normalizers/
├── matchers/
├── config/
├── output_match/
├── output_match_results/
├── output_json/
├── run_build_local.py
├── run_stage_match.py
