### 今やるべき見直しポイント（事前アドバイス）

#### ① normalize_comma_abb.py 側
チェックポイント：
✔ 本当に「ローカル前処理専用」になっているか
✔ list を必ず返す設計が守られているか
✔ overseas は関係ない（ここでは扱わない）
✔ 略語展開だけに責務が限定されているか

特に大事なのは：
「1→N候補生成エンジン」に完全特化しているか

#### 司令塔① 側
確認ポイント(特に重要)：
✔ "修正後欧文地名" を読むのはここだけか？
✔ normalized_name を list 型で保存しているか？
✔ overseas_territories.json を正しく使っているか？
✔ 二重正規化が起きていないか？

設計のゴール_3層分離が最強
```
司令塔① ＝ 候補生成装置（1→N）
司令塔② ＝ 段階比較装置（N→M）
距離フェーズ ＝ 最終決定装置（M→1）
```

ひとつだけ注意
normalize_comma_abb は
* 「地名の意味を変える処理（順番入替）」を含んでいる
* これはローカル専用。
* GeoNames側に絶対かけない。

---
#### normalize_comma_abb.py のレビュー
良い点:
✔ list を必ず返す設計
✔ 1→N候補生成専用になっている
✔ GeoNames側を触らない設計
✔ 辞書ロードはモジュール時1回のみ
設計思想は完全に正しい

改善ポイント（重要）:
① product を import しているが未使用 
→ 使っていないので削除
② カンマ順入替の責務
これはローカル専用なので正しい。
今の順番：
```
fj展開
→ ピリオド補正
→ カンマ入替
→ 略語展開
```
この順番は意味がある。
特に**ピリオド補正 → カンマ入替 → 略語展開**は崩さない方がいい。

---
#### 司令塔①のレビュー
今の設計では：
```
Excel（ローカル）
↓ normalize_comma_abb（1→N）
GeoNames辞書ヒット
↓
judge判定
```
これは「候補生成＋辞書ヒット司令塔」であり、  
Stage比較はまだやらない  
これが明確で良い。

問題点（設計的に）:
① PIPELINE_COMMON は不要
```
PIPELINE_COMMON = [
    normalize_comma_abb,
]
```
将来増やす予定がないなら不要。
なぜなら：
normalize_comma_abb はすでに 1→N 完結処理。

パイプライン化は設計的には正しいけど、  
今は一段しかない。

シンプルに：
```
df["normalized_name"] = df[TARGET_COL].astype(str).apply(
    normalize_comma_abb
)
```

② normalize_pipeline は過剰設計
```
def normalize_pipeline(name, pipeline):
```
将来用だけど、今の設計では使わなくていい。  
スリム化した方が良い。

③ match_geonames_candidates は正しい
```
for name in normalized_list:
    if name in placename_dict:
```
list対応もOK。  
複数ヒットも取得できる。  
距離フェーズ前提として完璧

④ judge計算
これは司令塔①として正しい。
* 0 → 未ヒット
* 1 → 一意
* 2+ → 複数
この段階では絞らない。正しい。

---

**司令塔①を軽量化**
削除できるもの：
* normalize_pipeline
* PIPELINE_COMMON
* 未使用import
* コメントアウトされた旧関数
* 過剰なworld統計

全体構造の確認
```
司令塔①
  ├ normalize_comma_abb（ローカル専用）
  ├ overseas対応
  ├ GeoNames辞書ヒット
  └ judge付与
      ↓
output_match
      ↓
司令塔②（stage_matcher）
      ↓
距離フェーズ
```

司令塔①：
* ローカルだけ変換
* GeoNamesはそのまま
* 複数ヒットOK
* overseas対応
* stage比較なし

→完全に責務分離できている。


✅ 司令塔① → 1→N候補生成＋辞書ヒット

✅ list完全対応

✅ overseas対応

✅ normalize_comma_abb → sum廃止で安定


確認するところ
* normalized_name が list になっているか
* geonames_hits が list のまま保持されているか
```
DataFrame
  ├ geonames_hits 列
      ├ 行0 → [ {dict}, {dict}, ... ]
      ├ 行1 → [ {dict}, ... ]
      ├ 行2 → []

```
.iloc[0] は 行0の値そのもの（= list）


* judge が正しく 0 / 1 / 2+ に分かれているか
* world_stats.xlsx の割合が想定どおりか